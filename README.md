# HINTBOT

This repo provides a reference implementation of **HINTBOT** as described in the paper:
> HINTBOT: 
> 
> Submitted for publication.

## Basic Usage

### Requirements

The code was tested with Python 3.5, `tensorflow-gpu` 1.7 and Cuda 9.0. Install the dependencies:

```python
pip install -r requirements.txt
```

### How to run the code
```shell
> 
> 
> 
> 
```

#### Options
You may change the model settings manually in `model.py` or directly into the codes. 

#### Datasets


